# JSON-HD HDF5 Profile (stub)

This profile will define:
- where the manifest lives inside an HDF5 container,
- how payloads map to datasets/groups,
- how `ref.type="dataset"` resolves,
- partial I/O expectations and recommended dataset chunking.

Status: draft placeholder.
