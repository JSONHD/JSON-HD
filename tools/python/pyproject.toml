[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jsonhd"
version = "0.1.0"
description = "JSON-HD reference tools (draft)"
requires-python = ">=3.9"
dependencies = ["jsonschema>=4.18.0"]

[project.scripts]
jsonhd = "jsonhd.cli:main"
