# JSON-HD Python Tools (draft)

This folder contains a minimal reference validator CLI for JSON-HD samples.

## Install (editable)
pip install -e .

## Run
python -m jsonhd.cli ../../samples/zip-minimal/manifest.json
